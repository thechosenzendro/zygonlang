using http, html.(p)

hello(context, name):
    p("Hello, {name}!")

router(route, context):
    case route:
        {"hello", "world"}: hello(context, "world")

http.server(router)
http.serve(_, port: 8080)